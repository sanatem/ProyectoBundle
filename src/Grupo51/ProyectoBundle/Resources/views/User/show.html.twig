{% extends 'Grupo51ProyectoBundle:LoggedUser:home.html.twig' %}

{% block content %}
    <h3>User</h3>

    <table class="table table-centered">
        <tbody>
            <tr>
                <th>Username</th>
                <td>{{ entity.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ entity.email }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
            <td>
                    <a href="{{ path('user') }}">
                      <span class="label label-info"> Back to the list </span>
        </a>
        </td>
        <td>
        <a href="{{ path('user_edit', { 'id': entity.id }) }}">
                       <span class="label label-info"> Edit </span>
        </a>
            </td>
            <tr>
        </tbody>
    </table>
                    {{form_start(delete_form)}}
                    {{ form_widget(delete_form.submit,{'attr':{'id':'form_submit','class':'btn btn-info'}} ) }}
 
                    {{form_end(delete_form)}}
{% endblock %}
